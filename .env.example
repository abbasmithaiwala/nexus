# SpaceTimeDB connection URL (local dev default: ws://localhost:3000)
VITE_SPACETIMEDB_URL=ws://localhost:3000

# SpaceTimeDB module name (must match the name used in `spacetime publish`)
VITE_SPACETIMEDB_MODULE=nexus
