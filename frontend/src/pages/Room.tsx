/**
 * RoomPage — the live meeting room.
 */

export function RoomPage() {
  return (
    <div className="min-h-screen bg-gray-950 flex items-center justify-center">
      <p className="text-gray-400">Meeting Room (Coming Soon)</p>
    </div>
  );
}
