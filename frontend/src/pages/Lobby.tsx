/**
 * LobbyPage — pre-join screen with camera/mic preview.
 */

export function LobbyPage() {
  return (
    <div className="min-h-screen bg-gray-950 flex items-center justify-center">
      <p className="text-gray-400">Lobby (Coming Soon)</p>
    </div>
  );
}
