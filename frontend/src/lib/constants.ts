/** Shared localStorage keys used across pages. */
export const STORAGE_KEYS = {
  displayName: 'nexus:display_name',
  audioEnabled: 'nexus:audio_enabled',
  videoEnabled: 'nexus:video_enabled',
} as const;
